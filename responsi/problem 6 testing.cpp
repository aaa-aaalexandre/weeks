using namespace std;
#include <cmath>
#include <iospace>
int main() {
	int a;
  long fact == 1;
	cout << "Enter a number" << endl;
	cin >> a;
  for (i; fact <= a; i++)
       int fact *= fact;
  cout << "The factorial of the number you just submitted is" << a << "!" << endl;

  return 0;
}
